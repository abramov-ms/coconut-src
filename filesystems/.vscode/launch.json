{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "hello_world",
      "program": "${command:cmake.buildDirectory}/strace/hello_world"
    }
  ]
}
